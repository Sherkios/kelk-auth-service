generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgresql:password@localhost:5432/kelk-auth?schema=public"
}
